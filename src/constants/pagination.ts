/**
 * Pagination and list limit constants
 */

// Default pagination limit for most lists
export const DEFAULT_PAGINATION_LIMIT = 50;

// Board view pagination limit (higher for kanban)
export const BOARD_PAGINATION_LIMIT = 100;

// Sidebar items limit
export const SIDEBAR_ITEMS_LIMIT = 20;

// Command palette results limit
export const COMMAND_PALETTE_LIMIT = 8;

// Analytics items limit
export const ANALYTICS_ITEMS_LIMIT = 10;

// Notifications limit
export const NOTIFICATIONS_LIMIT = 10;

// Time tracking entries limit
export const TIME_ENTRIES_LIMIT = 50;

// Automations list limit
export const AUTOMATIONS_LIMIT = 50;

// Cycle list limit
export const CYCLE_LIST_LIMIT = 50;

// Project list limit
export const PROJECT_LIST_LIMIT = 50;

// Issue list limit
export const ISSUE_LIST_LIMIT = 50;

// Pagination cursor limit max
export const PAGINATION_MAX_LIMIT = 100;

// Pagination cursor limit min
export const PAGINATION_MIN_LIMIT = 1;

// Sidebar display limit (how many items to show)
export const SIDEBAR_DISPLAY_LIMIT = 5;
