/**
 * Timeout and interval values (in milliseconds)
 */

// Success message display duration
export const SUCCESS_MESSAGE_TIMEOUT_MS = 3000;

// Copied state reset duration
export const COPIED_STATE_TIMEOUT_MS = 2000;

// Branch copy feedback duration
export const BRANCH_COPY_TIMEOUT_MS = 2000;

// Session cleanup interval (30 seconds)
export const SESSION_CLEANUP_INTERVAL_MS = 30000;

// Stale session threshold (1 minute)
export const STALE_SESSION_THRESHOLD_MS = 60000;

// Default debounce delay for search inputs
export const SEARCH_DEBOUNCE_MS = 300;

// Toast notification duration
export const TOAST_DURATION_MS = 5000;

// Loading state delay (to prevent flickering)
export const LOADING_DELAY_MS = 150;
